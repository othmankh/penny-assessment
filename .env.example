# Copy to .env.local and adjust (restart `npm run dev` after changes).
# Only VITE_* variables are exposed to the browser — never put secrets here.

VITE_HAND_SIZE=3
VITE_WIN_SCORE_DELTA=1
VITE_LOSS_SCORE_DELTA=-1
VITE_MAX_DRAW_EXHAUSTIONS=3
VITE_MIN_TILE_VALUE=0
VITE_MAX_TILE_VALUE=10
